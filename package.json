{
  "name": "armenian-legal-chat",
  "private": true,
  "description": "Root convenience wrapper — the actual frontend lives in frontend/. Running npm commands from here (instead of cd frontend first) is a common mistake (npm ERR! ENOENT looking for package.json at the repo root); these scripts make that work anyway instead of failing.",
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
