# Նախագծի Փաստաթղթավորում

## Ընդհանուր Տեսություն
Այս պահոցը պարունակում է հայերեն իրավական չաթ հավելվածը, իրական ժամանակի տեսողական վերլուծության հայտարարը և աջակցող փաստաթղթավորումը։ Այս փաստաթուղթը ներկայացնում է նախագծի աջակցվող հնարավորությունները և վերջին որակները՝ ներառյալ տեսախցիկի կենդանի հոսքը և ազդեցության հայտնաբերումը։

## Նախագծի Ամփոփում
- Ավելացվել է համատեղ տեսողական դասակարգիչ `src/services/vision_classifier.py`-ում։
- Ավելացվել է հայերեն մարմնի լեզվի և ակտիվության հայտնաբերման աջակցություն `src/services/vision.py`-ում։
- Ավելացվել է էմոցիոնալ հայտնաբերում՝ դեմքի նշանների և հոմանիշ գրեթե թեստավորման միջոցով։
- Ավելացվել է իրական ժամանակում տեսախցիկի աղբյուրի ընտրություն `src/main.py`-ում տեղական վեբքեմի և IP/հեռախոսային հոսքի համար։
- Փաստաթղթավորվել է `0`, `http://...` և `rtsp://...` տեսախցիկի աղբյուրների օգտագործումը։
- Փաստաթղթավորվել է GitHub հեռավոր պահոցը `https://github.com/Tsovinar1986/armenian_legal_chat.git`։

## Իրականացված Թարմացումներ
- Ամփոփվել են վերջին կենդանի տեսախցիկի և ցանցային հոսքի ռեժիմները։
- Հայերեն գործողությունների հայտնաբերում՝ ներառյալ ձեռքեր գոտկատեղին, ցուցադրում, քայլում, վազում և այլ դիրքեր։
- Էմոցիոնալ հայտնաբերում դեմքի արտահայտությունների միջոցով։
- Թարմացվել են փաստաթղթավորությունները՝ իրական ժամանակի տեսողական գործառույթները կամրապնդելու համար։

## Վերջին Փոփոխություններ
- Ավելացվել է տեսախցիկի աղբյուրի հարցման և IP հոսքի կարգավորման հնարավորությունը `src/main.py`-ում։
- Ընդլայնվել են գործողությունների հայտնաբերման հյուսթիքները `src/services/vision.py`-ում։
- Թարմացվել են `README.md`, `DOCUMENTATION.md` և `DOCUMENTATION_hy.md` ֆայլերը նոր գործառույթների մասին։
- Հեռացվել է առանձին `[l]awyer` ստեղնաշարի կառավարումը CLI-ից. այժմ ամենահաջողակ փաստաբանը (ըստ հաստատված գործերի քանակի) ավտոմատ կերպով ցուցադրվում է `LegalAgent.get_advice()`-ի պատասխանում։
- Ուղղվել է սխալ, երբ `src/main.py`-ը դասակարգիչը (`LegalCaseClassifier`) նախաստորագրում էր դատարկ, ավտոմատ ստեղծված `data/` պանակով, փոխարենը՝ իրական տվյալները պարունակող `src/data/`-ով, ինչի պատճառով որոնումները միշտ վերադարձնում էին ոչինչ։
- Ավելացվել է բրաուզերի չաթի API (`POST /api/chat`, `GET /api/chat/{session_id}`) FastAPI պորտալում (`api.py`), որը կապակցված է CLI-ի կողմից օգտագործվող նույն `LegalAgent`-ին։
- Ավելացվել է իրական բազմաշրջափուլ (multi-turn) զրույցի հիշողություն. `LegalAgent.get_advice()`-ն այժմ ընդունում է `history` ցուցակ, ներառում է նախորդ հարցերը որոնման հարցումում և LLM-ի հուշման մեջ։ Կապակցված է և՛ CLI-ին (`src/main.py`), և՛ վեբ չաթին (`api.py`)։
- Փոխարինվել են պորտալի հիշողության մեջ պահվող `users_db`/`bookings_db`-ն (որոնք կորչում էին վերագործարկման ժամանակ և գաղտնաբառերը պահում էին բաց տեքստով)՝ SQLite-ի վրա հիմնված պահպանմամբ (`src/db/portal_store.py`), որտեղ գաղտնաբառերը պահվում են աղակալված (salted) PBKDF2 hash-երով։
- Հեռացվել են առանձին `[s]imilar` և `[a]pproved` CLI կառավարումները։ Այդ գործառույթներն այժմ ավտոմատ կերպով ցուցադրվում են `LegalAgent.get_advice()`-ի պատասխանում՝ "📚 Նմանատիպ գործեր" բաժնում, որը ցույց է տալիս մինչև 3 այլ նմանատիպ գործ՝ փաստաբանի անունով և ✅ հաստատված նշանով, և՛ դասակարգիչի համընկնման ուղում, և՛ RAG fallback ուղում։ `LegalCaseClassifier.find_multiple_similar_cases()`-ն այժմ ավելացնում է `is_approved` դաշտ յուրաքանչյուր գործի համար։
- `LegalVisionService`-ը (`src/services/vision.py`) այժմ ծանրաբեռնված տեսողական մոդելները (`VisionClassifier`՝ PyTorch + YOLOv8 + MediaPipe Pose/FaceMesh) բեռնում է hետաձգված (lazy) եղանակով՝ միայն առաջին իրական կադրի կամ վերբեռնված տեսանյութի ժամանակ, այլ ոչ թե անվերապահորեն կառուցման պահին։ Միայն տեքստով/ձայնով զրույցի սեսիաները (ներառյալ վեբ չաթի բոլոր հարցումները) այլևս չեն վճարում այդ import/հիշողության գինը. չափված է ~268ՄԲ խնայողություն 8ԳԲ MacBook-ի վրա, որն արդեն մոտ էր swap-ի սահմանին։
- Ավելացվել է ճգնաժամային/անվտանգային հայտնաբերում. `src/services/crisis_detection.py`-ն տրամադրում է `detect_crisis_signal()` (արագ հայերեն/անգլերեն բանալի-բառերի ստուգում) և `CRISIS_RESPONSE_HY` (իրական արտակարգ իրավիճակների կոնտակտներով պատասխան)։ Կապակցված է որպես Քայլ 0 `LegalAgent.get_advice()`-ում, բոլոր մյուս ստուգումներից առաջ, այնպես որ գործում է և՛ CLI-ում, և՛ `/api/chat`-ում։
- Ավելացվել է zero-shot մտավոր առողջության ռիսկի դասակարգում՝ որպես երկրորդ, ավելի ծանր ազդանշան բանալի-բառերի ստուգման կողքին. `LegalCaseClassifier.classify_mental_health_risk()` (`src/services/classifier.py`) օգտագործում է hետաձգված բեռնվող HuggingFace `transformers` zero-shot pipeline (`MoritzLaurer/mDeBERTa-v3-base-mnli-xnli`)՝ հաղորդագրությունը դասակարգելու `["suicide or self-harm risk", "acute emotional or mental health crisis", "general legal question", "casual conversation"]` պիտակների դեմ։ Այն միայն ընդլայնում է ընդգրկումը (հայտնաբերում է այն ձևակերպումները, որոնք բանալի-բառերի ցուցակը բաց է թողնում)՝ երբեք չեղարկելով բանալի-բառի հայտնաբերումը, և ցածր վստահության կամ ձախողված դասակարգումը լուռ անցնում է սովորական իրավական խորհրդատվության մշակմանը։
- `LegalAgent.get_advice()`-ն այժմ ունի երկու ճգնաժամային ստուգում (Քայլ 0՝ բանալի-բառեր, Քայլ 0b՝ zero-shot) կարճ հարցի պարզաբանման ստուգումից և ողջ իրավական խորհրդատվության տրամաբանությունից առաջ։
- Ավելացվել են `transformers>=4.40` և `torch>=2.2` `requirements.txt`-ում zero-shot դասակարգիչի համար։
- Ավելացվել է մեղմ հոգեկան առողջության մտահոգության ազդանշան տեսախցիկի հոսքում. `LegalVisionService`-ը (`src/services/vision.py`) հետևում է վերջին 30 կադրերի էմոցիաների պատուհանին. եթե ≥60%-ը տխուր/բարկացած է, ցուցադրում է թերապևտի հետ խոսելու առաջարկություն էկրանին և սահմանում `SystemState`-ի նոր `mental_health_concern`/`mental_health_suggestion` դաշտերը (`src/core/state.py`)։ Զուտ դիտարկում է, ոչ ախտորոշում։
- Ավելացվել է `MentalHealthQAClassifier` (`src/services/classifier.py`)՝ TF-IDF որոնման դասակարգիչ՝ նոր տրամադրված `src/data/student_mh_counseling_100k_with_label_column.csv` տվյալների բազայի վրա (question/answer/label, ~100հազ տող. պիտակներ՝ depression, stress, seeking help, suicidal thoughts)։ Hետաձգված բեռնվում է միայն առաջին կիրառման ժամանակ, նույն սկզբունքով ինչ zero-shot ռիսկի մոդելը։ Սա թերապևտի կողմի համարժեքն է `LegalCaseClassifier`-ի, բայց տրամադրում է աջակցող Q&A որոնում, ոչ թե կոնկրետ թերապևտի հետ համապատասխանեցում (այդ մասը դեռ կառուցված չէ)։
- Ավելացվել են `POST /api/therapist-chat` և `GET /api/therapist-chat/{session_id}` `api.py`-ում՝ առանձին խոսակցության հոսք՝ հիմնված `MentalHealthQAClassifier`-ի վրա։ Կատարում է նույն ճգնաժամային ստուգումները մինչև Q&A դասակարգիչին դիմելը, քանի որ տվյալների բազան ինքնին պարունակում է "suicidal thoughts" պիտակով տողեր։
- Ավելացվել են Stripe-ի վրա հիմնված վճարումներ (քարտ, Apple Pay, Google Pay) և՛ իրավաբանական, և՛ թերապևտիկ խորհրդատվությունների համար. `payments` աղյուսակ `src/db/portal_store.py`-ում, `POST /api/payments/create-intent`, `GET /api/payments/{payment_id}`, `POST /api/payments/webhook` `api.py`-ում՝ կարգավորվող `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY`/`STRIPE_WEBHOOK_SECRET` միջավայրի փոփոխականներով։ Ավելացվել է `stripe>=9.0` `requirements.txt`-ում։
- Ավելացվել են backend-ready placeholder էջեր. `/pay` (Stripe վճարման ցուցադրական էջ), `/mood-tracking` (տրամադրության հետևման վեբ այլընտրանք, երբ բջջային հավելվածը հասանելի չէ), `/therapist` (աջակցող չաթի ցուցադրություն + հղում իրական սեսիա գրանցելու/վճարելու համար)։
- Ընդլայնվել են ամրագրումները (bookings) թերապևտիկ խորհրդատվությունները ընդգրկելու համար՝ առանց առկա API-ն կոտրելու. `bookings.provider_type` դաշտ (լռելյայն՝ `'lawyer'`)՝ `POST /api/bookings`-ը այժմ ընդունում է `provider_type: "lawyer" | "therapist"`։ Գրանցում/մուտք արդեն ընդունում էին ցանկացած `role` արժեք, ուստի `role: "therapist"`-ը backend փոփոխություն չպահանջեց։
- Ավելացվել է ամրագրման օրացույցի ազատ/զբաղված ժամերի ցուցադրում՝ և՛ տեղական ժամանակով, և՛ UTC-ով. նոր `bookings.timezone` և `bookings.start_time_utc` սյուներ (idempotent միգրացիա, `start_time`-ը մնում է անփոփոխ)։ `portal_store.start_time_to_utc_iso()`-ն նորմալացնում է ամրագրման ժամանակը UTC-ի stdlib `zoneinfo`-ի միջոցով (նոր կախվածություն չի պահանջվում)։ Նոր `GET /api/bookings/availability`-ն հաշվարկում է ֆիքսված աշխատանքային ժամերի (09:00–18:00, կարգավորելի) միջակայքեր տրված տեղական օրվա համար և նշում է յուրաքանչյուր միջակայքի ազատ/զբաղված վիճակը՝ վերադարձնելով և՛ տեղական, և՛ UTC ժամանակները։ Ցուցադրական ամրագրման քարտին ավելացվել է "Check free times" վահանակ (ինքնաբերաբար հայտնաբերում է դիտարկչի ժամային գոտին)։
- Փոխարինվել է `langchain-chroma`-ն ուղղակի `chromadb` wrapper-ով. `src/db/vector_store.py`-ն (`ChromaVectorStore`, `Document`) իրականացնում է langchain-ի `Chroma` ինտերֆեյսի այն փոքր ենթաբազմությունը, որն այս նախագիծն իրականում օգտագործում է (`similarity_search`, `similarity_search_with_score`, `add_texts`), այնպես որ `src/agents/legal_agent.py`-ն և `src/services/ingestion.py`-ն ոչինչ չփոփոխեցին։ Պատճառը. `crewai`-ն կոշտ ամրագրում է `chromadb~=1.1.0`, ինչը հակասում է `langchain-chroma`-ի `chromadb>=1.3.5,<2.0.0` պահանջին։
- Ընդունվել է CrewAI-ն բազմագործակալ չաթի պատասխանների համար. `src/agents/legal_crew.py` (`run_legal_crew`) և `src/agents/therapist_crew.py` (`run_therapist_crew`)՝ յուրաքանչյուրը երկու գործակալից (Researcher, Writer) բաղկացած հաջորդական `Crew`, օգտագործելով `crewai.LLM(model="ollama/<model_name>")`-ը առկա տեղական Ollama մոդելի հետ։ Երկու կանչման կետերն էլ բռնում են crew-ի բացառությունները և վերադառնում են crew-ից առաջվա վարքագծին (template պատասխան / ուղղակի QA որոնում), այնպես որ բացակայող `crewai` տեղադրումը կամ crew-ի ձախողումը softly է դեգրադացվում, ոչ թե սխալ է նետում։
  - **Researcher գործակալները չեն ստանում tools։** `armenia-lawyer-router`-ը (տեղական Ollama մոդելը) վերադարձնում է 400 `does not support tools` սխալ, եթե crewai `Agent`-ին տրվում է որևէ `tools=[...]`։ Որոնումը մնում է հենց այնտեղ, որտեղ արդեն կար՝ պարզ Python, crew-ի աշխատանքից առաջ, և փոխանցվում է researcher-ին որպես task նկարագրության տեքստ։
  - **`crewai`-ն միտումնավոր `requirements.txt`-ի սովորական տող չէ։** Հաստատված է metadata ստուգումով 1.0.0-ից մինչև 1.15.2 բոլոր տարբերակների վրա. բոլորը ամրագրում են `chromadb~=1.1.0`։ Հաստատված է live թեստով. `chromadb<1.2`-ը չի կարող բացել `chromadb>=1.5`-ով գրված `./chroma_legal_data` պանակը (Rust panic, ոչ մաքուր սխալ)։ Աշխատող տեղադրումը երկու քայլ է. `pip install -r requirements.txt`, ապա առանձին `pip install --no-deps crewai==1.15.2`։
- Ավելացվել է `LICENSE` (բոլոր իրավունքները պաշտպանված են, ոչ MIT) — միտումնավոր ընտրություն, քանի որ այս կոդբազան կարող է վաճառվել կամ լիցենզավորվել բացառիկ կերպով. MIT լիցենզիան կտար անսահմանափակ առևտրային վերաօգտագործման իրավունք ցանկացած մարդու։
- Ավելացվել են ֆայլի սկզբի մեկնաբանություններ և՛ `api.py`-ում, և՛ `src/main.py`-ում՝ պարզաբանելով, որ դրանք երկու տարբեր մուտքի կետեր են (FastAPI վեբ պորտալ ընդդեմ desktop CLI)՝ կիսելով նույն հիմքային `src/` կոդը, ոչ թե նույն ֆայլի երկու տարբերակներ։

## Պահոցի Նշումներ
- Թիրախային հեռավոր պահոցը՝ https://github.com/Tsovinar1986/armenian_legal_chat.git
- Փոփոխությունները պետք է commit արվեն և push արվեն ավարտելուց հետո։

## Հաջորդ Քայլեր
- Փորձարկել կենդանի տեսախցիկի հոսքը ինչպես տեղական վեբքեմով, այնպես էլ IP/հեռախոսային աղբյուրով։
- Օբյեկտների հայտնաբերման և տեսարանի ֆունկցիոնալությունը շարունակաբար բարելավել։
- Ավելի զարգացնել հայերեն տեսողական արձագանքը և փաստաթղթավորումը։
